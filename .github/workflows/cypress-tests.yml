name: 🚀 Cypress E2E Tests Pipeline

on:
  push:
    branches: [main, develop, feature/*]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:
    inputs:
      test_suite:
        description: 'Select test suite to run'
        required: false
        default: 'all'
        type: choice
        options:
          - all
          - saucedemo
          - realworld
          - api
      browser:
        description: 'Browser to use'
        required: false
        default: 'chrome'
        type: choice
        options:
          - chrome
          - firefox
          - edge
          - electron

env:
  NODE_VERSION: '18'
  CYPRESS_CACHE_FOLDER: ~/.cache/cypress

jobs:
  # Job 1: Setup and prepare
  setup:
    name: 📦 Setup & Install Dependencies
    runs-on: ubuntu-latest
    outputs:
      cypress-version: ${{ steps.cypress-version.outputs.version }}
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📚 Cache Cypress binary
        uses: actions/cache@v4
        with:
          path: ~/.cache/cypress
          key: cypress-${{ runner.os }}-${{ hashFiles('package.json') }}
          restore-keys: |
            cypress-${{ runner.os }}-

      - name: 🔨 Install dependencies
        run: npm install

      - name: 📊 Get Cypress version
        id: cypress-version
        run: echo "version=$(npm list cypress --depth=0 | grep cypress@ | sed 's/.*@//')" >> $GITHUB_OUTPUT

      - name: 📦 Cache node_modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('package.json') }}

  # Job 2: SauceDemo UI Tests
  saucedemo-tests:
    name: 🛒 SauceDemo E2E Tests
    runs-on: ubuntu-latest
    needs: setup
    if: ${{ !github.event.inputs.test_suite || github.event.inputs.test_suite == 'all' || github.event.inputs.test_suite == 'saucedemo' }}
    strategy:
      fail-fast: false
      matrix:
        containers: [1, 2, 3]
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📚 Restore Cypress cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/cypress
          key: cypress-${{ runner.os }}-${{ hashFiles('package.json') }}

      - name: 📦 Restore node_modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('package.json') }}

      - name: 🧪 Run SauceDemo Tests (Container ${{ matrix.containers }})
        uses: cypress-io/github-action@v6
        with:
          install: false
          spec: cypress/e2e/*.js
          browser: ${{ github.event.inputs.browser || 'chrome' }}
          record: false
        env:
          CYPRESS_BASE_URL: https://www.saucedemo.com

      - name: 📸 Upload Screenshots (on failure)
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: saucedemo-screenshots-${{ matrix.containers }}
          path: cypress/screenshots
          if-no-files-found: ignore
          retention-days: 7

      - name: 🎥 Upload Videos
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: saucedemo-videos-${{ matrix.containers }}
          path: cypress/videos
          if-no-files-found: ignore
          retention-days: 7

      - name: 📊 Upload Test Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: saucedemo-reports-${{ matrix.containers }}
          path: cypress/reports
          if-no-files-found: ignore
          retention-days: 30

  # Job 3: RealWorld UI Tests
  realworld-tests:
    name: 🌍 RealWorld Conduit Tests
    runs-on: ubuntu-latest
    needs: setup
    if: ${{ !github.event.inputs.test_suite || github.event.inputs.test_suite == 'all' || github.event.inputs.test_suite == 'realworld' }}
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📚 Restore Cypress cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/cypress
          key: cypress-${{ runner.os }}-${{ hashFiles('package.json') }}

      - name: 📦 Restore node_modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('package.json') }}

      - name: 🧪 Run RealWorld Tests
        uses: cypress-io/github-action@v6
        with:
          install: false
          spec: cypress/e2e/realworld/*.js
          browser: ${{ github.event.inputs.browser || 'chrome' }}
          record: false
        continue-on-error: true

      - name: 📸 Upload Screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: realworld-screenshots
          path: cypress/screenshots
          if-no-files-found: ignore
          retention-days: 7

      - name: 🎥 Upload Videos
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: realworld-videos
          path: cypress/videos
          if-no-files-found: ignore
          retention-days: 7

      - name: 📊 Upload Test Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: realworld-reports
          path: cypress/reports
          if-no-files-found: ignore
          retention-days: 30

  # Job 4: API Tests
  api-tests:
    name: 🔌 API Integration Tests
    runs-on: ubuntu-latest
    needs: setup
    if: ${{ !github.event.inputs.test_suite || github.event.inputs.test_suite == 'all' || github.event.inputs.test_suite == 'api' }}
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📚 Restore Cypress cache
        uses: actions/cache@v4
        with:
          path: ~/.cache/cypress
          key: cypress-${{ runner.os }}-${{ hashFiles('package.json') }}

      - name: 📦 Restore node_modules
        uses: actions/cache@v4
        with:
          path: node_modules
          key: node-modules-${{ runner.os }}-${{ hashFiles('package.json') }}

      - name: 🧪 Run API Tests
        uses: cypress-io/github-action@v6
        with:
          install: false
          spec: cypress/e2e/api/*.js
          browser: chrome
          record: false

      - name: 📊 Upload API Test Reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: api-test-reports
          path: cypress/reports
          if-no-files-found: ignore
          retention-days: 30

  # Job 5: Generate Consolidated Report
  generate-report:
    name: 📈 Generate Test Report
    runs-on: ubuntu-latest
    needs: [saucedemo-tests, realworld-tests, api-tests]
    if: always()
    steps:
      - name: 📥 Checkout code
        uses: actions/checkout@v4

      - name: 🔧 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: 📦 Install report dependencies
        run: npm install -g mochawesome-merge mochawesome-report-generator

      - name: 📥 Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts

      - name: 📊 Merge Mochawesome reports
        run: |
          mkdir -p consolidated-reports
          find ./artifacts -name '*.json' -type f -exec cp {} consolidated-reports/ \;
          npx mochawesome-merge "consolidated-reports/*.json" > consolidated-reports/merged-report.json || echo "No reports to merge"
        continue-on-error: true

      - name: 🎨 Generate HTML report
        run: |
          if [ -f "consolidated-reports/merged-report.json" ]; then
            npx mochawesome-report-generator consolidated-reports/merged-report.json \
              --reportDir consolidated-reports/html \
              --reportTitle "E2E Test Results - Cypress" \
              --reportPageTitle "Test Report" \
              --inline
          fi
        continue-on-error: true

      - name: 📊 Create Test Summary
        run: |
          echo "# 🧪 E2E Test Results Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📊 Test Execution Details" >> $GITHUB_STEP_SUMMARY
          echo "- **Branch:** \`${{ github.ref_name }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Commit:** \`${{ github.sha }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- **Triggered by:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Cypress Version:** ${{ needs.setup.outputs.cypress-version }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Node Version:** ${{ env.NODE_VERSION }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 🎯 Test Suites" >> $GITHUB_STEP_SUMMARY
          echo "| Suite | Status |" >> $GITHUB_STEP_SUMMARY
          echo "|-------|--------|" >> $GITHUB_STEP_SUMMARY
          echo "| 🛒 SauceDemo | ${{ needs.saucedemo-tests.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🌍 RealWorld | ${{ needs.realworld-tests.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "| 🔌 API Tests | ${{ needs.api-tests.result }} |" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📦 Artifacts" >> $GITHUB_STEP_SUMMARY
          echo "- Screenshots (on failures)" >> $GITHUB_STEP_SUMMARY
          echo "- Test execution videos" >> $GITHUB_STEP_SUMMARY
          echo "- HTML test reports" >> $GITHUB_STEP_SUMMARY
          echo "- Raw test data (JSON)" >> $GITHUB_STEP_SUMMARY

      - name: 📤 Upload Consolidated Report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 📊-consolidated-test-report
          path: |
            consolidated-reports/html
            consolidated-reports/merged-report.json
          retention-days: 30

      - name: 🎉 Upload All Screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 📸-all-screenshots
          path: ./artifacts/*screenshots*
          if-no-files-found: ignore
          retention-days: 7

      - name: 🎬 Upload All Videos
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: 🎥-all-videos
          path: ./artifacts/*videos*
          if-no-files-found: ignore
          retention-days: 7

  # Job 6: Performance Metrics
  performance-metrics:
    name: ⚡ Performance Metrics
    runs-on: ubuntu-latest
    needs: [saucedemo-tests, realworld-tests, api-tests]
    if: always()
    steps:
      - name: 📊 Calculate Metrics
        run: |
          echo "# ⚡ Performance Metrics" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 📈 Execution Statistics" >> $GITHUB_STEP_SUMMARY
          echo "- **Total Test Suites:** 3" >> $GITHUB_STEP_SUMMARY
          echo "- **Total Tests:** ~505 tests" >> $GITHUB_STEP_SUMMARY
          echo "- **Parallel Containers:** 3 (SauceDemo)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 🎯 Test Distribution" >> $GITHUB_STEP_SUMMARY
          echo "- SauceDemo UI Tests: ~300 tests" >> $GITHUB_STEP_SUMMARY
          echo "- RealWorld UI Tests: ~105 tests" >> $GITHUB_STEP_SUMMARY
          echo "- API Tests: ~100 tests" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## 💡 Optimization Tips" >> $GITHUB_STEP_SUMMARY
          echo "- Consider migrating to Playwright for 60% faster execution" >> $GITHUB_STEP_SUMMARY
          echo "- Current estimated runtime: ~45 minutes" >> $GITHUB_STEP_SUMMARY
          echo "- Potential with Playwright: ~18 minutes" >> $GITHUB_STEP_SUMMARY

  # Job 7: Notify on completion
  notify:
    name: 📢 Notifications
    runs-on: ubuntu-latest
    needs: [saucedemo-tests, realworld-tests, api-tests, generate-report]
    if: always()
    steps:
      - name: 📢 Create Status Badge
        run: |
          if [ "${{ needs.saucedemo-tests.result }}" == "success" ] && \
             [ "${{ needs.realworld-tests.result }}" == "success" ] && \
             [ "${{ needs.api-tests.result }}" == "success" ]; then
            echo "STATUS=✅ All Tests Passed" >> $GITHUB_ENV
            echo "COLOR=green" >> $GITHUB_ENV
          else
            echo "STATUS=⚠️ Some Tests Failed" >> $GITHUB_ENV
            echo "COLOR=red" >> $GITHUB_ENV
          fi

      - name: 📋 Final Summary
        run: |
          echo "# 🎯 Pipeline Completion" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "## Overall Status: ${{ env.STATUS }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 📁 Available Artifacts:" >> $GITHUB_STEP_SUMMARY
          echo "1. 📊 Consolidated HTML Test Report" >> $GITHUB_STEP_SUMMARY
          echo "2. 📸 Test Failure Screenshots" >> $GITHUB_STEP_SUMMARY
          echo "3. 🎥 Test Execution Videos" >> $GITHUB_STEP_SUMMARY
          echo "4. 📝 Raw Test Data (JSON)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### 🔗 Quick Links:" >> $GITHUB_STEP_SUMMARY
          echo "- [View Workflow Run](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }})" >> $GITHUB_STEP_SUMMARY
          echo "- [Download Artifacts](https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}#artifacts)" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "---" >> $GITHUB_STEP_SUMMARY
          echo "*Generated by GitHub Actions on $(date)*" >> $GITHUB_STEP_SUMMARY
